[project]
name = "rezerwacja-sal"
version = "0.1.0"
description = "System rezerwacji sal"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2.8",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-django>=4.8.0",
    "pytest-bdd>=7.0.0",
    "pyppeteer>=2.0.0",
]